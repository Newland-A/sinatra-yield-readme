# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }


gem 'sinatra'
gem 'rake'
gem 'thin'
gem 'shotgun'
gem 'require_all'

#I ran bundle init to create the gemfile
#then added my required gems basics without the code

# group :test do
#   gem 'rspec'
#   gem 'capybara'
#   gem 'rack-test'
#   gem 'pry'
# end

#beacause there is no tests the run for this file but makes for great practice at creating files.
#then run bundle install to get gem dependencies.
